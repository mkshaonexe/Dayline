package com.day.line.ui.theme

import androidx.compose.ui.graphics.Color

// Brand Colors
val DaylineOrange = Color(0xFFFF8A00) // Refined Sunset Orange
val NeonOrange = Color(0xFFFF9E40)    // Vibrancy kept but harmonized
val SoftTeal = Color(0xFF4DB6AC)      // Secondary/Evening
val ElectricBlue = Color(0xFF2979FF)  // Accent for active states

// Glass & Surface Colors (Premium)
val GlassBlack = Color(0xCC111111)    // Dark Glass
val GlassWhite = Color(0xE6FFFFFF)    // Light Glass - increased opacity for better visibility
val GlassBorderLight = Color(0x40FFFFFF) // Subtle border for glass
val GlassBorderDark = Color(0x1FFFFFFF)  // Subtle border for dark glass

// Backgrounds
val CreamBackground = Color(0xFFF7F2E8) // Warmer, premium cream
val DeepSpaceBlack = Color(0xFF0A0A0A)  // Richer, deeper black for OLED
val DarkSurface = Color(0xFF141414)     // Slightly lighter black

// Text
val TextBlack = Color(0xFF1D1B20)
val TextWhite = Color(0xFFF2F2F2)
val TextGrey = Color(0xFF9E9E9E)

// Material 3 Mappings
val Primary = DaylineOrange
val Secondary = SoftTeal
val Tertiary = ElectricBlue

val BackgroundLight = CreamBackground
val BackgroundDark = DeepSpaceBlack
val SurfaceLight = Color.White
val SurfaceDark = DarkSurface

// Dayline Pastel Palette (Migrated)
val PastelRed = Color(0xFFE57373) // Approximation for the red/coral color
val PastelBlue = Color(0xFFB3E5FC) // Approximation for the light blue color
val PastelActionBlue = Color(0xFFE1F5FE) // Lighter blue for backgrounds
val PastelOrange = Color(0xFFFFCC80) // Approximation for orange
val PastelGreen = Color(0xFFA5D6A7) // Approximation for green
val PastelGrey = Color(0xFFF5F5F5) // Background grey
val TextDark = Color(0xFF2C2C2C)
val TextLight = Color(0xFF757575)
val TimelineLineColor = Color(0xFFE0E0E0)